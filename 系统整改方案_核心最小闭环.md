# USTB AI教务助手 - 系统整改方案（核心最小闭环）

基于项目当前状况与《项目复盘-管理框架重构.md》复盘结论，制定一次到位的系统整改方案，目标是用最小闭环架构快速恢复稳定、提升可维护性，并为后续优化留出清晰扩展位。

---

## 1. 目标与范围

- 保留并强化的核心组件（本地）：
  - 前端：`src/frontend`（localhost:3000）
  - 后端：`src/backend`（localhost:8001）
- 保留并强化的核心组件（AutoDL）：
  - RAG 检索服务：`rag_service_v2.py`（远端:8000）
  - 模型推理服务：`qwen_inference_server.py` 或统一的 `model_inference_server.py`（远端:8001）
- 连接组件（本地→AutoDL）：
  - SSH 隧道：`system/ssh_tunnel/setup_web_tunnel.py` 或 Windows 原生 SSH（多端口直转）

本次整改明确移除“Web API 连接器（8003）”层，采用“后端直连本地转发口”的简单拓扑，显著降低复杂度与不稳定因素。

---

## 2. 架构精简方案

### 2.1 新拓扑（3层最小闭环）

前端(3000) → 后端(8001) → 本地转发 → AutoDL（RAG:8000 / 模型:8001）

关键变化：
- 移除 8003 层；后端仅依赖本地两个转发口（8000/8004）。
- 避免端口冲突：远端模型保持 8001，本地映射为 8004（与后端 8001 解耦）。

### 2.2 端口统一与约束

- 本地固定：
  - 前端：3000
  - 后端：8001
  - RAG 转发：8000 → 远端 8000
  - 模型转发：8004 → 远端 8001
- 远端固定（AutoDL）：
  - RAG：8000
  - 模型：8001（如必须使用其它端口，请在本地映射与 `.env` 同步一致）

端口冲突保底方案：若本地 8000/8004 被占用，可切换到 18000/18004，并同步 `.env`。

---

## 3. 环境变量与配置统一

### 3.1 后端 `src/backend/.env` 模板

```bash
HOST=0.0.0.0
PORT=8001
RAG_API_URL=http://localhost:8000
MODEL_API_URL=http://localhost:8004
API_TIMEOUT=30.0
ALLOWED_HOSTS=*
```

说明：
- 后端仅调用本地转发端口；不再直连 8003。
- `MODEL_API_URL` 预留给后续模型直连或混合回答使用。

### 3.2 前端 `src/frontend/.env.local` 模板

```bash
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
```

说明：
- 前端仅调用后端，不直连 AutoDL。

---

## 4. SSH 隧道整改（多端口直转 + 密钥认证）

强制改为密钥认证，移除脚本中明文密码；使用 Windows 自带 OpenSSH 或现有脚本的密钥模式。

### 4.1 一键启动（PowerShell，后台启动）

```powershell
$sshArgs = '-N -L 8000:localhost:8000 -L 8004:localhost:8001 -p 21020 root@connect.bjb1.seetacloud.com'
Start-Process -FilePath ssh -ArgumentList $sshArgs -WindowStyle Hidden
```

如需关闭，结束对应 ssh 进程或重启系统。

### 4.2 健康检查（本地）

```powershell
Invoke-RestMethod "http://localhost:8000/health" -TimeoutSec 5 | ConvertTo-Json
Invoke-RestMethod "http://localhost:8004/health" -TimeoutSec 5 | ConvertTo-Json
```

### 4.3 脚本整改建议（如继续使用 `setup_web_tunnel.py`）

- 支持多端口：添加 `-L 8000:localhost:8000` 与 `-L 8004:localhost:8001` 两条映射。
- 密钥认证：增设 `-i <private_key_path>`；删除明文密码逻辑。
- 健康检查：统一探测 `/health` 并打印 `reachable` 与 `latency_ms`。

---

## 5. 组件侧整改要求

### 5.1 前端（Next.js / TS）

- 只调用后端（`${NEXT_PUBLIC_BACKEND_URL}`）；后端做聚合、重试与降级。
- 网络封装统一超时与取消（`AbortController`）。
- 关键界面数据校验与空态处理，避免空白屏。

### 5.2 后端（FastAPI / Python）

- 配置集中：读取 `.env` 的 `RAG_API_URL` 与 `MODEL_API_URL`，禁用 8003 任何引用。
- HTTP 客户端：全局 `httpx.AsyncClient`（连接池 + 超时 + 限流），应用生命周期内复用。
- 健康聚合：新增 `/health` 聚合本地 8000/8004 健康，返回 `reachable`、`latency_ms`、`version`、`model_name` 等。
- 降级策略：外部依赖异常时返回 `degrade=true` 且明确错误码映射（502/503/504）。
- CORS：开发阶段允许 `localhost:3000`；生产禁用 `*`。

### 5.3 RAG 服务（FastAPI）

- 统一健康接口 `/health` 字段：`status`、`version`、`gpu.available`、`documents_count`、`uptime`。
- 保持端口 8000；索引加载/缓存预热打印关键指标。

### 5.4 模型推理服务（FastAPI）

- 统一端口 8001（远端）；本地映射至 8004。
- 健康接口 `/health` 字段：`status`、`version`、`gpu.available`、`model_name`、`model_loaded`、`uptime`。

---

## 6. 日志与监控整改（统一标准）

### 6.1 日志格式（所有 Python/TS 组件）

- 建议 JSON Lines 或统一文本格式，包含：
  - `timestamp`、`level`、`service`、`request_id`
  - `method`、`path`、`status_code`、`latency_ms`
  - `remote_ip`、`error_code`、`message`

### 6.2 日志目录与轮转

- 本地：`logs/backend/*.log`、`logs/frontend/*.log`
- AutoDL：`/root/autodl-tmp/ustb-project/logs/{rag|model}_service.log`
- 轮转策略：按大小（10MB）或按天轮转，保留近 7 天；敏感信息脱敏。

### 6.3 后端接入（示例要点）

- 使用 `logging` + `uvicorn` 统一格式；为每次请求生成 `request_id` 并贯穿下游调用。
- 为外部 HTTP 调用增加埋点：目标 URL、耗时、重试次数、异常类型。

### 6.4 健康与可观测性

- 健康聚合 `/health` 返回：
  - `backend`: 端口、uptime
  - `rag`: `reachable`、`latency_ms`、`version`
  - `model`: `reachable`、`latency_ms`、`model_name`
- 后端定时（60s）内部自检一次，异常时打印 `WARN` 级别日志，便于告警接入。

---

## 7. 测试与验收清单

### 7.1 冒烟测试（本地）

- `GET http://localhost:8000/health` → 200 且 `status=ok`
- `GET http://localhost:8004/health` → 200 且 `status=ok`
- `GET http://localhost:8001/health` → 200 且 `rag/model.reachable=true`
- 前端聊天一次：返回文本 +（若有）附件链接

### 7.2 集成测试（后端→RAG/模型）

- RAG 搜索：`/api/v1/search` 正常；超时报错映射 504。
- 模型推理（如接入）：正常返回，超时/异常可降级到 RAG-only。

### 7.3 性能与稳定（最小要求）

- 健康探测 100% 通过；
- RAG 平均响应 < 500ms（或当前客观水平，记录基线）；
- 模型推理 < 2s（训练完成与部署后验证）。

---

## 8. 文件管理与清理（源自复盘教训）

### 8.1 规则

1) 测试文件、临时文件、正式文件严格分离：
   - 测试：`tests/`；临时：`temp/`；正式：`src/`、`docs/`、`configs/`
2) 新版本创建时删除旧版本：禁止多版本服务并存（如 `rag_service_*` 保留唯一权威版本）。
3) 每周清理过期临时文件与日志归档：`temp/`、`logs/`。

### 8.2 建议保留与重命名

- RAG 服务：保留 `rag_service_v2.py`（或移动到 `src/services/rag_system/` 作为权威实现）。
- 模型服务：保留 `model_inference_server.py`（明确端口与健康接口）；删除过期 `*_fixed.py` 版本。

---

## 9. 安全整改

- SSH 一律改密钥认证；禁止明文密码。
- CORS 在生产限域；`ALLOWED_HOSTS` 明确域名清单。
- 机密通过环境变量注入，不入库、不入 Git。
- 后端对外错误信息屏蔽堆栈；日志脱敏。

---

## 10. 代码规范（落地版）

### 10.1 Python（后端 / RAG / 模型）

- 类型：公共 API 与关键函数强制类型注解。
- 风格：Black（88列）+ Ruff（flake8）+ isort 导入分组。
- 结构：路由/controllers → 服务/services → 适配/adapters → 模型/schemas。
- 控制流：早返回、异常分级、对外依赖全部有超时与重试上限。
- 日志：结构化日志，携带 `request_id` 与 `latency_ms`。

### 10.2 TypeScript（前端）

- TSConfig：`"strict": true`；拒绝 `any`。
- Lint：ESLint + Prettier；import 顺序与别名统一。
- 网络：统一 fetch 封装，超时、重试、取消；错误提示友好。
- 状态：必要时用全局 store；其它优先局部 state。

---

## 11. 启动顺序（稳定版）

1) AutoDL 启动 RAG(8000) 与 模型(8001)。
2) 本地启动 SSH 隧道（映射 8000、8004）。
3) 本地启动后端（8001），再启动前端（3000）。
4) 运行第 7 节冒烟测试四步，确保链路可用。

---

## 12. 实施计划（T+2 天达成）

### D0（当天）

- 移除 8003 相关配置与文档表述；
- 后端 `.env` 写入统一配置（3.1）；
- SSH 隧道改为多端口直转，完成健康检查（4.1/4.2）。

### D1

- 后端新增 `/health` 聚合（若未有），补齐日志字段；
- 清理多版本服务文件，仅保留权威实现；
- 前端统一读取 `NEXT_PUBLIC_BACKEND_URL`，回归冒烟测试通过。

### D2

- 集成测试脚本完善（RAG/模型/降级路径）；
- 完成日志轮转与周清理任务；
- 输出《系统验收报告（精简版）》：功能 / 性能 / 稳定性 结果。

---

## 13. 风险与回滚

- 端口占用：切换本地 8000/8004 → 18000/18004，同步 `.env`。
- 模型远端端口差异：本地映射口与 `.env` 保持一致；不依赖固定数值。
- 隧道不稳定：重连策略 + 本地健康探测告警；保留手动 SSH 命令快速替代脚本。

---

## 14. 附录

### 14.1 一次性自检命令（PowerShell）

```powershell
# 1) 建立双端口隧道（后台）
$sshArgs = '-N -L 8000:localhost:8000 -L 8004:localhost:8001 -p 21020 root@connect.bjb1.seetacloud.com'
Start-Process -FilePath ssh -ArgumentList $sshArgs -WindowStyle Hidden

# 2) 健康检查
Invoke-RestMethod "http://localhost:8000/health" -TimeoutSec 5 | ConvertTo-Json
Invoke-RestMethod "http://localhost:8004/health" -TimeoutSec 5 | ConvertTo-Json
Invoke-RestMethod "http://localhost:8001/health" -TimeoutSec 5 | ConvertTo-Json
```

### 14.2 `.env` 模板速查

后端：

```bash
HOST=0.0.0.0
PORT=8001
RAG_API_URL=http://localhost:8000
MODEL_API_URL=http://localhost:8004
API_TIMEOUT=30.0
ALLOWED_HOSTS=*
```

前端：

```bash
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
```

---

## 15. 复盘经验内化（摘自《项目复盘-管理框架重构.md》并落地）

1) 计划先行与小步验证：先做原型与健康链路验证，再扩展层级；本方案即是“3层最小闭环”。
2) 文件规范与定期清理：权威版本唯一性、周清理机制，避免技术债；本方案第 8 节明确规则。
3) 权限与风险控制：SSH 改密钥、禁明文密码；后端错误映射与降级策略，减少单点崩溃影响面。
4) 文档职责分离：本“整改方案”作为统筹规划；运行与状态放在后端 `/health` 与《系统验收报告》里实时/阶段呈现。

---

以上整改完成后，项目以“前端→后端→SSH直连RAG/模型”的三层最小闭环稳定运行，复杂度下降、可维护性上升。后续若需扩展混合回答、负载均衡或监控告警，可在不改变最小闭环的前提下逐步引入。

---

## 16. 代码级整改指导（仅文档，不直接改代码）

本节将“应修改的位置、推荐代码片段、验证步骤”一次性写清。按需执行，建议在新分支完成并逐项自测。

### 16.1 快速定位清单（需要移除/替换的 8003 残留）

- 后端：
  - `src/backend/app.py`
    - startup 中对 `http://localhost:8003/health` 的健康检查
    - `/health` 接口中对 8003 的检查与返回字段
  - `src/backend/routes/chat.py`
    - `call_hybrid_api` 使用 `http://localhost:8003/api/v1/chat`
  - `src/backend/config.py`
    - `HYBRID_API_URL` 默认 `http://localhost:8003`
- 文档：
  - `docs/project/USTB项目8专家分工详细文档.md`、`docs/technical/USTB混合架构升级版技术方案.md` 中关于“混合API(8003)”的描述（文字层更新即可）

建议用如下命令定位（Windows PowerShell）：

```powershell
rg "8003" -n
```

### 16.2 配置与常量（后端）

- 文件：`src/backend/config.py`
  - 保持并使用：
    - `RAG_API_URL` 默认 `http://localhost:8000`
    - `RAG_API_TIMEOUT` 默认 `30.0`
  - 新增并使用（用于后续模型直连或混合回答）：
    - `MODEL_API_URL` 默认 `http://localhost:8004`
    - `MODEL_API_TIMEOUT` 默认 `30.0`
  - 处理 `HYBRID_API_URL`：不再默认指向 `8003`，可设为空字符串或仅保留占位，不在代码路径中使用。

示例（仅供参考，实际请在分支中编辑）：

```python
# RAG直连
RAG_API_URL = os.getenv("RAG_API_URL", "http://localhost:8000")
RAG_API_TIMEOUT = float(os.getenv("RAG_API_TIMEOUT", "30.0"))

# 模型直连（本地8004→远端8001）
MODEL_API_URL = os.getenv("MODEL_API_URL", "http://localhost:8004")
MODEL_API_TIMEOUT = float(os.getenv("MODEL_API_TIMEOUT", "30.0"))

# 混合API（弃用8003，保留占位以兼容旧代码）
HYBRID_API_URL = os.getenv("HYBRID_API_URL", "")
```

### 16.3 健康检查聚合（后端 `app.py`）

- startup 阶段：去掉对 8003 的检查，改为检查 `localhost:8000`（RAG）与 `localhost:8004`（模型，若暂未启用可忽略失败）。
- `/health`：返回 `rag` 与 `model` 的聚合状态，不再含 `hybrid_api`。

参考代码片段（仅文档示例）：

```python
# startup 中
rag_health = await http_client.get("http://localhost:8000/health", timeout=5.0)
model_health = await http_client.get("http://localhost:8004/health", timeout=5.0)

# /health 中
services_status = {}
for name, url in {"rag": "http://localhost:8000/health", "model": "http://localhost:8004/health"}.items():
    try:
        r = await http_client.get(url, timeout=5.0)
        services_status[name] = "healthy" if r.status_code == 200 else "unhealthy"
    except Exception:
        services_status[name] = "unavailable"
```

验证点：
- `GET http://localhost:8001/health` 返回 JSON 中仅包含 `rag` 与 `model` 字段。
- SSH 隧道未启时，`model` 允许为 `unavailable`；RAG 启动后为 `healthy`。

### 16.4 聊天路由改造（`routes/chat.py`）

现状使用 `call_hybrid_api` 直打 `http://localhost:8003/api/v1/chat`。在移除 8003 方案下，最简替代有两种：

- 方案 A（最小改动，先可用）：仅调用 RAG 检索并格式化为回答。
- 方案 B（预留混合）：先尝试模型 `MODEL_API_URL`，失败或低置信度时回落到 RAG，再合并附件。

方案 A 的参考片段（仅文档示例）：

```python
async def call_rag_only(self, query: str) -> Dict:
    req = {"query": query, "top_k": 5}
    r = await self.http_client.post(f"{config.RAG_API_URL}/api/v1/search", json=req, timeout=30.0)
    r.raise_for_status()
    return r.json()

async def process_chat_message(...):
    rag_json = await self.call_rag_only(request.message)
    # 将 rag_json["results"] 转为文本回答 + 附件清单
```

注意：`format_hybrid_response` 可复用，将来源从混合API结果换为 RAG 结果结构（字段名可能不同，需在拼装时转换）。

### 16.5 分类路由（`routes/categories.py`）

已通过 `config.RAG_API_URL` 读取 `http://localhost:8000`，无需依赖 8003；保持不变或补充超时与兜底策略即可。

### 16.6 隧道脚本与密钥

- 文件：`system/ssh_tunnel/setup_web_tunnel.py`
  - 将 `-L` 改为双端口映射：`8000:localhost:8000` 与 `8004:localhost:8001`
  - 改为密钥认证：添加 `-i <private_key_path>`，删除明文密码写入逻辑
  - 健康检查从 `http://localhost:8003/health` 改为检查 `8000` 与 `8004`

参考命令（PowerShell 后台）：

```powershell
$sshArgs = '-N -L 8000:localhost:8000 -L 8004:localhost:8001 -p 21020 root@connect.bjb1.seetacloud.com'
Start-Process -FilePath ssh -ArgumentList $sshArgs -WindowStyle Hidden
```

### 16.7 自测与验收

1) 启动 AutoDL：RAG(8000) 与 模型(8001)。
2) 启本地隧道：本地 8000/8004 对应远端 8000/8001。
3) 启后端(8001) 与前端(3000)。
4) 自检：
   - `GET http://localhost:8000/health` → 200
   - `GET http://localhost:8004/health` → 200（如未启模型可忽略）
   - `GET http://localhost:8001/health` → `rag/model` 聚合状态
   - 发起一次聊天 → 有文本回应；若实现了附件整形，应能看到附件链接

### 16.8 日志与可观测性（具化）

- 统一字段：`timestamp`、`level`、`service`、`request_id`、`method`、`path`、`status_code`、`latency_ms`、`message`。
- 后端增加请求级 `request_id`（ASGI 中间件产生），并贯穿外呼到 RAG/模型的日志前缀。
- 失败时统一映射：`Timeout → 504`，`ConnectError → 503`，其它未捕获 → `500`，并返回 `degrade=true`（若设计到聚合层）。

---

## 17. 变更执行建议（不改主干）

- 新建分支：`feature/min-core-closure`。
- 提交粒度：
  1) 删除 8003 残留（仅后端）
  2) 健康聚合改造
  3) 聊天路由最小可用（RAG-only）
  4) 隧道脚本与文档更新
- 每步通过本文第 7 节与第 16.7 节的冒烟与聚合健康检查。



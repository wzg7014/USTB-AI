## 28. 统一聚合接口契约（/api/v1/answer）

- 目标：以最小改造实现“RAG→模型只读总结”的闭环；对外提供稳定契约，Quick 与 Core 共用该契约。
- 方法：后端聚合新增 `POST /api/v1/answer`，内部按策略调用 RAG(8000)+模型(可选)，并统一格式化输出。

- 请求体（建议字段，全部可选除 `query` 外）：
  - `query: string`（必填）
  - `top_k: number`（默认 5，≤10）
  - `category_filter: string[]`（可选）
  - `date_range: [string, string]`（ISO 日期，或自然语言经解析后透传）
  - `include_attachments: boolean`（默认 true）
  - `flags: object`（策略透传，覆盖环境变量）
    - `use_rerank?: boolean`
    - `freshness_alpha?: number`、`freshness_tau_days?: number`
    - `context_tokens?: number`、`model_temperature?: number`、`model_max_tokens?: number`

- 响应体（合同化结构）：
  - `answer: string`（遵循“先结论→要点/步骤→注意事项→参考文献”模板）
  - `citations: Array<{ id: string; title: string; url: string; publish_date?: string; snippet?: string; score?: number }>`（≤5，标题与 URL 对齐）
  - `attachments: Array<{ title: string; url: string; content_type?: string }>`（可为空；需 HEAD 可用性校验）
  - `meta: { request_id: string; degrade: boolean; cached: boolean; rag_latency_ms: number; model_latency_ms?: number; total_latency_ms: number; params: object; version: string }`

- 错误返回（统一）：
  - `status: number`、`code: string`（如 `RAG_TIMEOUT`/`MODEL_FAIL`）、`message: string`、`request_id: string`

- 约束：
  - 最多 5 条引用；`answer` 必须包含参考文献编号并与 `citations` 一一对应。
  - 若模型失败，`degrade=true`，仅返回“RAG 要点+引用”。


## 29. 编排引擎与策略（LangGraph 薄编排，可回退）

- 引擎选型：
  - 主用 `LangGraph` 实现薄编排（状态清晰、节点可观测、易扩展）。
  - 回退 `LangChain`（线性 Runnable）。
  - 再回退 `INLINE`（内联过程式，不依赖任何编排库）。

- 编排节点：
  1) `normalize`：规范化与自然时间解析（“近N天/月/学期/今年”→`date_range`）
  2) `rag_search`：RAG 检索（Top-K 扩大候选→后处理）
  3) `postprocess`：
     - 去重：URL 精确 + 标题相似（阈值≈0.9）
     - 新鲜度加权：`score' = score * (1 + α · e^{-days/τ})`
     - 片段抽取：句级抽取+滑窗，拼接 ≤ `CONTEXT_TOKENS`
  4) `rerank`（可选一层）：外部 API（Cohere/Jina），失败回退原排序
  5) `summarize`：严格模板（先结论→要点→注意事项→参考文献）
  6) `format`：输出 `answer/citations/attachments/meta`
  7) `degrade`：模型失败→仅 RAG 要点 + 引用

- 可用性与平台：
  - Windows 友好：无本地 Torch 依赖；重排优先外部 API。
  - `LangServe` 可选（后续将编排图服务化），当前不强制。

- 性能与稳定：
  - httpx 统一超时/重试/熔断；RAG 超时=15s、模型=30s（默认）。
  - 单层重排；仅保留后端缓存 60s（避免多级缓存失配）。
  - 所有外呼与阶段埋点时长进入 `meta` 与指标。


## 30. 配置与开关（12-Factor，环境变量优先）

- 开关（与 35.3 保持一致）：
  - `ANSWER_MODE` ∈ {`QUICK`,`CORE`}（默认 `QUICK`）
  - `RAG_ENTRY` ∈ {`DIRECT_8000`,`HYBRID_8002`}（Core 固定 `DIRECT_8000`）
  - `MODEL_ENTRY` ∈ {`DIRECT`,`DISABLED`}（Core 受策略控制）
  - `USE_RERANK` ∈ {`true`,`false`}（默认 `false`）
  - `ORCH_ENGINE` ∈ {`LANGGRAPH`,`LANGCHAIN`,`INLINE`}（Core 推荐 `LANGGRAPH`）
  - `RERANK_PROVIDER` ∈ {`COHERE`,`JINA`}（默认 `COHERE`）
  - `FRESHNESS_ALPHA`（默认 `0.5`）、`FRESHNESS_TAU_DAYS`（通知 `45–90`、规章 `90–120`）
  - `CONTEXT_TOKENS`（默认 `1200`）
  - `MODEL_TEMPERATURE`（默认 `0.2`）、`MODEL_MAX_TOKENS`（默认 `512`）
  - `RAG_TIMEOUT_SEC`（默认 `15`）、`MODEL_TIMEOUT_SEC`（默认 `30`）
  - `MAX_CITATIONS`（默认 `5`）、`ATTACHMENT_LINK_CHECK`（默认 `true`）

- 端点与资源：
  - `RAG_API_URL`（默认 `http://localhost:8000`）
  - `HYBRID_API_URL`（默认 `http://localhost:8003`，仅 QUICK 回滚用）
  - `USE_MODEL_GATEWAY` ∈ {`true`,`false`}（默认 `false`）
  - `LITELLM_BASE_URL`、`LITELLM_API_KEY`（开启网关时必填）
  - `MODEL_BASE_URL`、`MODEL_API_KEY`（直连模型时）
  - `COHERE_API_KEY`、`JINA_API_KEY`（根据 `RERANK_PROVIDER` 选择其一）
  - `DATABASE_URL`、`REDIS_URL`

- 可观测：
  - `OTEL_EXPORTER_OTLP_ENDPOINT`、`PROM_BIND_ADDR`

- 安全：
  - `SECRET_KEY`、各类 `*_API_KEY`（禁止硬编码；提供 `.env.example`）


## 31. 可观测性基线（指标/追踪/日志）

- 指标（Prometheus `/metrics`）：
  - `rag_latency_ms`、`model_latency_ms`、`total_latency_ms`
  - `rag_qps`、`answer_qps`、`error_rate`、`cache_hit_ratio`、`hit_at_3`
  - 端点维度：`endpoint="/api/v1/answer"`、`status`、`degrade`

- 追踪（OpenTelemetry）：
  - FastAPI/httpx 自动注入；根 span 绑定 `request_id/user_id/ANSWER_MODE/USE_RERANK`
  - 关键阶段作为子 span：`normalize`、`rag_search`、`postprocess`、`rerank`、`summarize`

- 日志（JSON 结构化）：
  - 字段：`ts/level/request_id/user_id/endpoint/latency_ms/error_code`
  - 产线仅 JSON；敏感字段脱敏；与 traceId 关联


## 32. 安全与合规基线

- 凭据：全部来源于环境变量或 Secret 管理（Vault/KMS）；禁止提交明文到仓库
- 鉴权：Bearer/JWT +（可选）RBAC；接口级速率限制（用户/IP/端点）
- CORS：严格白名单；前端启用 CSP/HSTS/Referrer-Policy
- PII 脱敏：对学号/手机号/邮箱做识别与掩码（可用 Presidio）
- 审计：登录/变更/导出/失败调用写入审计日志；错误码表统一


## 33. 实施与迁移（最小闭环）

- D0（1–2 天）
  - 新增后端聚合 `POST /api/v1/answer`（第28节契约）。
  - 新增 `src/backend/orchestrator/graph.py`（`LangGraph` 薄编排；`ORCH_ENGINE` 可回退 `LANGCHAIN/INLINE`）。
  - 统一 `config.py`，移除 `app.py` 内部重复 `Config` 与所有硬编码端点/凭据。
  - 外呼统一 httpx 超时/重试/熔断；新增 `/metrics`、请求 ID 中间件。
  - `.env.example`、开关矩阵接管；保留 8003 仅作回滚。

- D1（3–5 天）
  - 引入模型网关（LiteLLM），统一限流/审计/重试（`USE_MODEL_GATEWAY=true`）。
  - 接入外部重排 Cohere（或 Jina），Windows 友好；失败回退原排序。
  - 接入 OpenTelemetry（trace/metric）与结构化日志；完善错误码与降级路径
  - 修复数据库连接一致性与会话接口边界；完善 A/B（`USE_RERANK`、`α/τ` 网格）

- D2（5–10 天）
  - 容器化与 CI/CD：镜像/健康检查/依赖扫描；一键 compose；逐步迁移到 K8s（含 API 网关）
  - 移除历史 8003；回滚脚本保留（只切换环境变量即可）

- 回滚：`ANSWER_MODE=QUICK`，必要时 `RAG_ENTRY=HYBRID_8002`；恢复旧链路不动代码


### 33.1 D0 一页执行单（操作清单/自检/回滚）

- 前置条件：
  - 已准备 `.env`（可从 `.env.example` 复制）并设置：
    - `ANSWER_MODE=CORE`
    - `ORCH_ENGINE=LANGGRAPH`（可回退 `LANGCHAIN/INLINE`）
    - `RAG_API_URL=http://localhost:8000`
    - `RAG_TIMEOUT_SEC=15`、`MODEL_TIMEOUT_SEC=30`
    - `FRESHNESS_ALPHA=0.5`、`FRESHNESS_TAU_DAYS=90`、`CONTEXT_TOKENS=1200`
    - `MAX_CITATIONS=5`、`ATTACHMENT_LINK_CHECK=true`
    - 生产环境补充：`SECRET_KEY`、`DATABASE_URL`、`REDIS_URL`、`OTEL_EXPORTER_OTLP_ENDPOINT`

- 变更文件（代码实现时）：
  1) `src/backend/orchestrator/graph.py`（新增）
     - 定义节点：`normalize/rag_search/postprocess/rerank/summarize/format/degrade`
     - 所有阶段接受/返回字典，记录阶段耗时供指标使用
  2) `src/backend/routes/answer.py`（新增）
     - `POST /api/v1/answer` 读取环境+flags，调用编排图并按契约返回
  3) `src/backend/app.py`（修改）
     - 注册 `answer` 路由；新增 `/metrics`；请求ID中间件
  4) `src/backend/config.py`（修改）
     - 统一读取所有环境变量与开关；删除硬编码与重复 `Config`
  5) `.env.example`（新增/补全）

- 操作步骤：
  1) 合并并生效 `.env` 配置
  2) 启动 RAG(8000) → 启动后端(8001)
  3) 切 `ANSWER_MODE=CORE`、`ORCH_ENGINE=LANGGRAPH`

- 自检清单（全部通过才算 D0 完成）：
  - 接口：
    - `POST /api/v1/answer` 返回 `answer/citations(≤5)/attachments/meta`，引用编号与 `citations` 一致
  - 指标与追踪：
    - 访问 `/metrics` 可见 `rag_latency_ms/model_latency_ms/total_latency_ms/error_rate`
    - 日志含 `request_id`；trace（若配置 OTEL）看到各阶段子 span
  - 开关：
    - 切换 `ORCH_ENGINE=LANGCHAIN/INLINE` 行为稳定；`USE_RERANK=false` 正常（D0 不接重排）
  - 降级：
    - 模型失败时仍输出“RAG 要点+引用”（`meta.degrade=true`）
  - 安全：
    - 仓库无明文凭据；CORS 白名单生效

- 回滚（任一步骤异常）：
  - `ANSWER_MODE=QUICK`；必要时 `RAG_ENTRY=HYBRID_8002`，无需改代码即可恢复旧链路

### 33.2 D1 一页执行单（模型网关/重排/观测增强）

- 目标：在不改对外契约的前提下，引入模型网关与外部重排，提高鲁棒性与质量；完善可观测性细节。

- 依赖（示例）：
  - 模型网关：`litellm`
  - 重排（2选1）：`cohere` 或 `jina-ai-client`
  - OTel（若未装）：`opentelemetry-sdk`、`opentelemetry-exporter-otlp`、`opentelemetry-instrumentation-fastapi`、`opentelemetry-instrumentation-httpx`

- 环境变量（在 `.env` 中新增/启用）：
  - `USE_MODEL_GATEWAY=true`
  - `LITELLM_BASE_URL`、`LITELLM_API_KEY`（启用网关时必填）
  - （直连兜底）`MODEL_BASE_URL`、`MODEL_API_KEY`
  - `USE_RERANK=true`、`RERANK_PROVIDER=COHERE|JINA`
  - `COHERE_API_KEY` 或 `JINA_API_KEY`
  - 观测：`OTEL_EXPORTER_OTLP_ENDPOINT`（若上报到 Tempo/Jaeger）

- 变更文件（代码实现时）：
  1) `src/backend/orchestrator/graph.py`
     - 在 `summarize` 节点支持通过 LiteLLM/直连调用模型（按 `USE_MODEL_GATEWAY` 选择）
     - 在 `rerank` 节点接入 `Cohere/Jina`（按 `RERANK_PROVIDER` 选择），失败回退原排序
     - 为 `rerank/model_infer` 增加单独的计时与错误捕获（写入 `meta`）
  2) `src/backend/config.py`
     - 新增/读取：`USE_MODEL_GATEWAY/LITELLM_BASE_URL/LITELLM_API_KEY/MODEL_BASE_URL/MODEL_API_KEY/USE_RERANK/RERANK_PROVIDER/COHERE_API_KEY/JINA_API_KEY`
  3) （可选）`src/backend/services/llm_client.py`、`src/backend/services/reranker.py`
     - 抽象模型与重排调用，便于单测与切换

- 操作步骤：
  1) 安装依赖，配置 `.env`，确保能访问 LiteLLM/重排 API
  2) 启动 RAG→后端；设置 `USE_MODEL_GATEWAY=true`、`USE_RERANK=true`
  3) 进行回归与 A/B：`USE_RERANK` 打开/关闭、`α/τ` 网格

- 自检清单：
  - `/api/v1/answer` 正常；`meta` 中包含 `model_latency_ms/rerank_latency_ms/degrade` 等信息
  - 指标：新增 `rerank_latency_ms`、`model_error_rate`；Trace 中可见 `rerank/model_infer` 子 span
  - A/B：`USE_RERANK` on/off 的 `hit@3` 与主观质量有差异（并记录）
  - 回退：`USE_MODEL_GATEWAY=false`、`USE_RERANK=false` 后系统可恢复 D0 行为

### 33.3 D2 一页执行单（容器化/CI·CD/K8s/网关）

- 目标：工程化交付。实现镜像、compose 一键起、CI·CD 安全门禁，逐步迁移到 K8s 与 API 网关。

- 依赖/工具：
  - 容器：Docker、多阶段镜像；`docker-compose`
  - CI·CD：GitHub Actions（lint/test/build/publish）、安全扫描 `pip-audit`、`trivy`
  - 代码规范：`ruff/black/mypy`
  - 压测（可选）：`k6` 或 `locust`

- 环境/配置：
  - 容器内仅使用环境变量；`PROM_BIND_ADDR`、`OTEL_EXPORTER_OTLP_ENDPOINT`
  - K8s：ConfigMap/Secret 管理 `.env`，HPA 指标来源 Prom/自定义

- 变更文件：
  1) `Dockerfile`（后端/RAG 各一份，非 root、只读 FS、健康检查端点）
  2) `docker-compose.yml`（后端+RAG+Redis/Qdrant 可选，一键起）
  3) `.github/workflows/ci.yml`（lint+test+build+scan+publish）
  4) （可选）`deploy/k8s/*`（Deployment/Service/Ingress 或 Gateway），`deploy/helm/*`

- 操作步骤：
  1) 本地构建并 `compose up` 自测，确认 `/health`、`/metrics` 正常
  2) 推送镜像到私有仓库，启用 CI·CD 门禁
  3) 上 K8s：创建命名空间→应用 Secret/ConfigMap→部署后端/RAG→接入 Ingress/API 网关（Kong/Traefik）
  4) 启用 HPA 与金丝雀/蓝绿（按需）；SLO 与报警接入

- 自检清单：
  - 镜像安全扫描无高危；容器健康检查绿；只读文件系统生效
  - K8s 就绪探针/存活探针通过；滚动发布无 5xx 尖峰
  - HPA 能在压测下自动扩缩容；SLO 报表可见；回滚一键可用

- 回滚：
  - Compose：`docker compose down` 后回到本地运行
  - K8s：`kubectl rollout undo deployment/<name>`，或切换上一个镜像 tag

## 34. 验收与 SLO/SLI

- 体验：
  - `answer` 模板落地；引用编号与 `citations` 对齐且可点击；附件可用率≥95%

- 质量：
  - 金标集 ≥20；`hit@3 ≥ 80%`；“最新且相关”命中；编造率低

- 性能：
  - `p95 total < 3s`（RAG≤800ms，模型≤2s）；错误率 < 1%

- 可观测：
  - 指标暴露完整；trace 覆盖关键阶段；日志字段完备（可关联 request_id）

- 安全：
  - 仓库无明文密钥；CORS 白名单；PII 脱敏；限流与审计开启

- 回归与 A/B：
  - RAGAS/DeepEval 作为回归门禁；重排开关与 `α/τ` 网格 A/B 报告

  

## 35. 双方案协同边界与开关矩阵（与《系统整改方案_立竿见影.md》互补）

本节用于确保两份方案“互补不重叠、可顺滑迁移、可一键回滚”。立竿见影版侧重“运行期参数与流程优化”，核心最小闭环版侧重“架构与代码级改造”。

### 35.1 方案定义与边界

- 立竿见影（Quick）：
  - 不改代码、不改拓扑；优先通过参数、提示词、检索排序与结果后处理提升效果。
  - 允许临时依赖混合API(8002)以减少改动；若不可用则直接用 RAG(8000)+模型(8001) 的现有接口组合。
  - 影响范围仅限：生成参数、超时、轻量重排、片段选取、时间理解、去重、引用对齐、附件链路校验、降级与指标、短期缓存。
  - 输出结构与核心方案保持一致（answer/citations/attachments/meta）。

- 核心最小闭环（Core）：
  - 架构与代码级改造：移除 8003、后端聚合 `/api/v1/answer`、健康聚合、日志与测试门禁、端口与配置统一。
  - 合同化接口与可观测性基线，作为长期权威落地。

### 35.2 职责划分（不重叠清单）

- Quick 负责（实施即生效）：
  - 生成参数基线：temperature=0.2、max_tokens=512、top_k=5、RAG/模型超时=15s/30s
  - 答案模板约束：先结论→要点/步骤→注意事项→参考文献
  - 轻量重排（前20→Cross-Encoder重排，失败回退原排序）
  - 片段选取（句级抽取+滑窗，≤1200 tokens）、自然时间解析→date_range
  - 新鲜度权重（α/τ 按业务调参）、URL+标题相似去重
  - 附件链接 HEAD 校验、文件名标准化；引用编号对齐（≤5）
  - 降级路径清晰、关键指标与60s缓存
  - 中文分词/正则适配、A/B（重排开关、α/τ）

- Core 负责（开发完成后启用）：
  - 后端聚合 `/api/v1/answer` 实现“RAG→模型只读检索文档总结”的闭环
  - 健康聚合、日志/指标标准化、契约与测试门禁、端口与配置统一
  - 移除 8003，端到端 SLO 与回滚脚本

### 35.3 开关矩阵（运维态，默认安全值）

以下为约定的环境变量与策略开关。Quick 阶段可通过“参数透传/调用约定”实现同等效果；Core 阶段由后端聚合读取并生效。

- ANSWER_MODE ∈ {QUICK, CORE}（默认 QUICK；核心聚合验收后切为 CORE）
- RAG_ENTRY ∈ {DIRECT_8000, HYBRID_8002}（QUICK 可用 HYBRID_8002；CORE 强制 DIRECT_8000）
- MODEL_ENTRY ∈ {DIRECT, DISABLED}（QUICK 可选；CORE 由 `/api/v1/answer` 策略控制，端点由 `MODEL_BASE_URL`/`MODEL_API_URL` 决定）
- USE_RERANK ∈ {true,false}（QUICK 默认 false，D2 后可 true；CORE 使用后端内部重排）
- FRESHNESS_ALPHA（默认 0.5）、FRESHNESS_TAU_DAYS（通知 45–90、规章 90–120）
- CONTEXT_TOKENS（默认 1200）
- MODEL_TEMPERATURE（默认 0.2）、MODEL_MAX_TOKENS（默认 512）
- RAG_TIMEOUT_SEC（默认 15）、MODEL_TIMEOUT_SEC（默认 30）
- MAX_CITATIONS（默认 5）、ATTACHMENT_LINK_CHECK ∈ {true,false}（默认 true）

注：上述变量名为规范占位，Quick 阶段可通过请求参数/配置文件模拟；Core 阶段在后端 `.env`/配置系统中正式接管。

### 35.4 执行顺序与迁移

1) 启用 Quick：按照《系统整改方案_立竿见影.md》D0→D2 执行，不动代码。
2) 上线 Core：新分支实现 `/api/v1/answer` 与健康/日志/测试；部署后将 ANSWER_MODE=CORE。
3) 渐退历史层：CORE 稳定运行一周后禁用 8003（保留回滚脚本）。

### 35.5 冲突防护（避免双重处理）

- 重排只允许一层：若 RAG 层已做重排，则后端聚合不再重复（由 USE_RERANK 控制）。
- 去重位置唯一：在RAG响应整形阶段完成，后续不再二次去重。
- 缓存只保留后端聚合 60s：前端不叠加缓存，避免失配。
- 时间权重统一：以后端聚合版为准；Quick 阶段按同一 α/τ 口径执行。

### 35.6 验收对齐

- 两方案共用同一验收口径：
  - 体验：先结论、要点清晰、参考文献编号与链接可点
  - 质量：命中“最新且相关”，附件可用率≥95%，hit@3≥80%
  - 性能：p95 总响应 < 3s（RAG≤800ms，模型≤2s）
  - 可观测：rag_latency、model_latency、total_latency、hit@3、缓存命中率

### 35.7 回滚策略

- 从 CORE 回滚到 QUICK：将 ANSWER_MODE=QUICK，必要时临时改用 RAG_ENTRY=HYBRID_8002；保留 8002 的回滚脚本。
- 从 QUICK 回到 CORE：恢复 ANSWER_MODE=CORE；确保 8000/8004 映射与健康聚合正常。

### 35.8 文档互链

- 本节与《系统整改方案_立竿见影.md》1–13 节一一对应；当存在冲突时，以本文件第28–34节（Core 聚合规范）为最终约束。

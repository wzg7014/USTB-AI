# USTB AI教务助手 - 系统整改方案（立竿见影版）

目标：在不改远端服务架构（AutoDL 8000/8001）的前提下，尽快显著提升“回复效果与稳定性”。该方案与《系统整改方案_核心最小闭环.md》并行，优先级更高，执行成本低，收益快。

---

## 1. 启动即效（无需改代码）

- 生成参数（后端/混合API均适用）
  - temperature=0.2
  - max_tokens=512（必要时 1024）
  - top_k=5
  - RAG 超时=15s、模型超时=30s
- 答案模板约束（提示词或后端模板）
  - 先结论
  - 后要点/步骤/注意事项
  - 末尾“参考文献：【1】【2】…（标题+链接+日期）”

即时调用示例（混合API 8002，可用作临时替代）
```json
POST http://localhost:8002/api/v1/hybrid_inference
{
  "query": "近三个月的选课通知",
  "strategy": "rag_primary",
  "max_tokens": 512,
  "temperature": 0.2,
  "include_attachments": true
}
```

---

## 2. 检索质量（轻量增强）

- 轻量重排（可选，HTTP 一层）
  - 对 RAG 前 20 结果做 Cross-Encoder 重排（如 bge-reranker-base 或外部API），取前 5 进入生成
  - 不改变核心链路；失败则使用原排序
- 片段选取（句级抽取+滑窗）
  - 按 query 命中与 BM25 得分拼接片段
  - 控制上下文 ≤ 1200 tokens（文档 200~300 字）
- 时间理解（自然时间解析）
  - “近N天/月/学期/今年”等解析为 date_range，默认近 180 天

---

## 3. 新鲜度与排序（RAG 层）

- 融合打分：score = (0.7·向量相似 + 0.3·BM25) · (1 + α · e^{-days/τ})
  - 初始 α=0.5，τ∈[60,90] 天
- 动态参数（业务细分）
  - 通知：α∈[0.5,0.8]、τ∈[45,90]
  - 规章：α∈[0.3,0.5]、τ∈[90,120]
- 去重：URL 精确 + 标题相似度>0.9 的近似去重

---

## 4. 附件与引用（可用性）

- 链接校验：
  - HEAD 探测 200 + Content-Type 过滤；坏链剔除
  - 附件名标准化（保留扩展名）
- 引用对齐：
  - 答案中的【序号】与 citations 一一对应（≤5 条）

---

## 5. 可靠性与可观测性

- 降级策略：
  - 模型失败 → 输出“RAG 要点 + 引用”，meta.degrade=true
  - RAG 超时 → 提示“检索繁忙，请稍后再试”，空引用
- 核心指标：
  - rag_latency_ms、model_latency_ms、total_latency_ms
  - hit@3（引用是否命中最新）、空日期占比
  - 60s 缓存命中率

---

## 6. 中文适配

- BM25 中文分词（jieba 或默认中文 Analyzer）
- 全角/半角、空白规范化
- 日期正则覆盖中文格式（yyyy年MM月dd日 等）

---

## 7. 评测与回归

- 金标问法集（不少于 20 条）：
  - “最新/近三个月/今年/截止日期/附件下载/缓考安排/转专业时间/学分认定”等
- 验证项：
  - 正确性、引用命中、附件可用率
  - p95 延迟；重排开关与 α/τ 网格 A/B

---

## 8. 渐退历史层（架构风险控制）

- 后端稳定直连 8000/8001 一周后，禁用 8003（Web API 连接器）
- 保留回滚脚本（启停 8002/8003 的一键开关）

---

## 9. 一页落地清单（D0–D2）

### D0（当天，1–2 小时）
- 下发生成参数（temperature/max_tokens/top_k/超时）
- 启用答案模板约束（先结论+参考文献）
- 启用 date_range 解析（“近N天/月/学期”）

### D1（次日，半天）
- 片段选取与上下文控制（≤1200 tokens）
- 附件链接校验与标准化；引用对齐
- 指标与日志打点（latency、hit@3、缓存率）

### D2（再下一日，可选）
- 轻量重排（对前 20 做 Cross-Encoder 重排）
- 动态 α/τ + 周期校准

---

## 10. 验收标准

- 体验：
  - 先结论，后要点；末尾“参考文献”可点开
  - 用户要求“附件表”时，能正确输出可用附件（≤5 条）
- 质量：
  - 命中“最新且相关”的通知；无明显编造
  - hit@3≥80%，附件可用率≥95%
- 性能：
  - p95 总响应 < 3s（RAG≤800ms，模型≤2s）

---

## 11. 立即可用的接口约定

- RAG（8000）
  - `POST /api/v1/search` → 支持 `top_k/category_filter/date_range`
- 模型（8001）
  - `POST /api/v1/inference` → 使用“严格模板”的拼接查询
- 混合API（8002，可临时使用）
  - `POST /api/v1/hybrid_inference`，`strategy=rag_primary`，参数同上

---

## 12. 风险与回滚

- 风险：重排/片段抽取增加延迟 → 仅对前 20 做重排；缓存 60s
- 回滚：关闭重排与时间加权；恢复原 RAG Top-K 直出

---

## 13. 与核心方案的边界

- 本方案不替代核心最小闭环（第28–34节），而是“立刻可做”的增益层
- 当核心新接口 `/api/v1/answer` 完成后，本方案中的参数与模板继续保留




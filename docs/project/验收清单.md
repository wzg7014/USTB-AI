# 阶段验收清单（可执行）

说明：功能/契约看 HTTP 与 JSON；数据看近 3 年；日志看可复盘；性能看 P95/错误率/并发。所有结果需落在指定路径。

## P0 合规与统一
- /health → 期望 200；services 反映 RAG 可用/降级 → 产物：logs/rag_flow/
- /v1/models → 含 ustb-* 模型 ID → 产物：tests/reports/M0_baseline.json
- /api/v1/search → total>0；publish_date 均在近 3 年 → 产物：data/governance/reports/data_quality_report.md
- /v1/chat/completions（非流） → 文尾附件与 URL；rag_results 非空 → 产物：logs/rag_flow/

## P1 联通与契约
- /v1/chat/completions（SSE） → 分块 + [DONE]；错误体统一 → 产物：tests/integration/api_contract.test.ts
- 旧 API 弃用 → 404/410 + 迁移指引 → 产物：docs/project/api-map.md

## P1.5 LangChain 最小链路
- LC 评估 → 召回@k/NDCG/延迟 对比 → 产物：tests/reports/M2_lc_eval.json

## P2 可观测与性能
- /metrics + 日志 → 指标齐备；日志按 request_id 可复盘 → 产物：logs/samples/
- 压测 → P95<3s、错误率<1%、50 并发 → 产物：tests/reports/M3_perf.json

---

## 结果记录（模板）
- 时间：
- 环境：
- 结论：通过/不通过
- 发现问题：
- 整改与回归：
